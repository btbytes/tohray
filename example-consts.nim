
# You MUST SET THESE VALUES before building

#const dbPath* = "/app/data/tohray.db"  # full path to sqlite. You can also use relative paths like "./tohray.db".
#const inviteCode* =         		# something to share with anyone that you are inviting to the site. don't make it too easy to guess.
#const secretKey* =          		# something that looks like a s3cr37-an8-4-l0ng-0n3-much-l00nger-.
#const siteName* =           		# "My Blog"
#const siteTitle* =          		# "Unbridled enthusiasm".. IDK
#const port* = 8080
